<h1>New user</h1>
<!-- <%#= render 'form' %> -->
<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name , :maxlength=> 10 %>
  </div>
 <div class="field">
    
    <%= f.hidden_field :leave , :value => 3 %>
  </div>
 
 <div class="actions">
    <%= f.submit  %>
  </div>
<%end%>
<br>
<%#= link_to 'Back', users_path %>
